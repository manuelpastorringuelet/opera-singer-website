import { Lora } from "next/font/google";
import { Montserrat } from "next/font/google";

export const lora = Lora({ subsets: ["latin"], preload: true });
export const montserrat = Montserrat({ subsets: ["latin"], preload: true });
